/*public list<int> numbers = new list<int>();
numbers.Add(1)
numbers.Add(2)
for (int i = 0; i < names.Count; i++)
        {
            Debug.Log("contents are: " + numbers[i]);
            //Console.WriteLine(numbers[i]);
        }*/
